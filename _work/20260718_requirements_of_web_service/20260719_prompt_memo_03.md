# 0. intoroduction

- 以下の回答mdファイルを参考に、ファイルを修正せよ
    - /loc0/bigbrother/repositories/causal-atelier/_work/20260718_requirements_of_web_service/20260719_prompt_memo_02_ansewer.md

- 提示してもらった以下の変更点に対応する
    1. `application/web`をuse case単位の名称へ変更する。
    2. API RouterからSQLAlchemy操作をApplication Serviceへ移動する。
    3. Metadata Repository、Unit of Work、Data Queryのportを追加する。
    4. `workers/executor.py`を状態管理・materialization・projectionへ分割する。
    5. `ArtifactLocation`を導入して`bucket`依存を弱める。
    6. Artifact Store factoryをregistry化する。
    7. 必要に応じてAzure BlobまたはDatabricks adapterを追加する。

- 矛盾/懸念点あれば確認せよ

# 1. ディレクトリ構成について

## 1.1. frontend

現状のままで良い

## 1.2. src/causal_atelier/application 配下の構成について

> application/web: workerも利用しているため、名称として不正確
> 現状でも呼び出しは成立するが、今後拡張するならuse case単位への再編を推奨

> 規模が拡大するなら、インターフェイスではなくuse case単位に整理する方が適切である。
```
application/
  pipeline/
    planning.py
    execution.py
    strategies.py
    validation.py
    discovery.py
    inference.py
    etl.py

  data_catalog/
    services.py

  configuration_catalog/
    services.py

  run_execution/
    services.py

  visualization/
    services.py

  ports/
    artifact_store.py
    data_query.py
    metadata_repository.py
    unit_of_work.py
    event_queue.py
```
こちらに則り、再編せよ

## 1.3. その他

以下、問題ありとみなされた点を修正せよ
- Application層がInfrastructureへ直接依存している
- API RouterがPersistence modelを直接操作している
- `workers/executor.py` の責務が大きい
- `infrastructure/artifacts` と `artifact_store` の名称が近い

# 2. infrastructure/artifact_store/ 配下の対応基盤（サービス）

- 以下の修正を行うこと

> AzureのcontainerやGCSのbucketへは対応付けられるが、名称が特定基盤寄りである。
> 
> より一般化するなら、次のようなlocatorが適切である。

```python
ArtifactLocation(
    backend="AZURE_BLOB",
    namespace="container-name",
    key="path/to/object",
    version="...",
)
```

> またfactoryを条件分岐ではなくregistry化すると拡張しやすくなる。

```python
ARTIFACT_STORE_FACTORIES = {
    "LOCAL": build_local,
    "S3": build_s3,
    "AZURE_BLOB": build_azure_blob,
    "DATABRICKS_VOLUME": build_databricks_volume,
}
```

- Azure Blob のみ実装せよ
