# 0. intoroduction

以下は作成されたソースコード郡に対する質問である。
修正案があっても許可が出るまでファイルは編集しないこと

# 1. ディレクトリ構成について

## 1.1. frontend

/loc0/bigbrother/repositories/causal-atelier/frontend
ここの責務はなにか？src配下に収められるのが適切なのではないか？

各ディレクトリとその中に格納されているソースコードの責務を明らかにせよ

## 1.2. src/causal_atelier/application 配下の構成について


/loc0/bigbrother/repositories/causal-atelier/src/causal_atelier/application/
配下の構成についてだが、

application/ 配下にベタで置かれたソースと、
application/web 配下に置かれたソースが存在する。

- application/
    - cli/
    - web/

と行った形で、インターフェイス別に分けるほうが責務として正しいのではないか？
共通的に使用するものがあれば、
common
と行った形で分けたほうが良い？
呼び出し側の見え方という観点から、混乱を来さないならば現状のままで良い

## 1.3. その他

その他、責務のレイヤとディレクトリのレイヤが一致しない構成があれば、明示せよ

# 2. infrastructure/artifact_store/ 配下の対応基盤（サービス）

- /loc0/bigbrother/repositories/causal-atelier/src/causal_atelier/infrastructure/artifact_store/
   - s3.py
についてだが、

Azure Blob
databricks
や
他のインフラとの接続も拡張可能か？
