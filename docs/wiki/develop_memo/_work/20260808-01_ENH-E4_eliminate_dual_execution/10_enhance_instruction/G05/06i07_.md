E4-G05 Trial 01 Phase C internal work package C1を継続せよ。

Current source of truth:

```text
docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_06i06.md
```

Gate / Trial / Phase / Work packageは変更しない。

```text
Gate         = E4-G05
Trial        = 01
Phase        = C
Work package = C1
```

今回はstandard PostgreSQL E2Eを再実行する前に、06i06で追加したArtifact derivation lineage修正のsemantic correctnessを確認せよ。

現在追加済みと報告されている:

```text
FITTED_PREPROCESSOR 全 Artifact × PARTITION_INDEX 全 Artifact
FITTED_MODEL        全 Artifact × FITTED_PREPROCESSOR 全 Artifact
PREDICTION          全 Artifact × FITTED_MODEL 全 Artifact
```

への一律 `DERIVED_FROM` persistenceを、単にE2Eを通す目的で確定してはならない。

`DERIVED_FROM` はactual provenanceを表さなければならない。

## 1. まずArtifact cardinalityを確定する

対象Predictive Golden Pathで、各ArtifactTypeについてactual persistence件数を確認する。

```text
PARTITION_INDEX
FITTED_PREPROCESSOR
FITTED_MODEL
PREDICTION
```

以下を明示する。

```text
- 各typeが何件persistされるか
- 同一ArtifactTypeが複数persistされる理由
- 各Artifactがどのrunner draft / Result / StageExecutionから生成されたか
- duplicateなのか、semanticに異なる複数Artifactなのか
```

## 2. 正しいprovenance contractを選択する

### Case A — このGolden Pathでは各type 1件が正しい場合

duplicate Artifact persistenceをproduction defectとして修正する。

その場合、E2Eにも最低限:

```text
len(artifacts) == 4
各ArtifactType exactly one
```

相当のcardinality assertionを追加し、dictによるduplicate collapseで不具合を隠さないこと。

そのうえでactual chain:

```text
PARTITION_INDEX
    <- DERIVED_FROM - FITTED_PREPROCESSOR
    <- DERIVED_FROM - FITTED_MODEL
    <- DERIVED_FROM - PREDICTION
```

を正しいArtifact ID同士で保存する。

### Case B — 同一ArtifactType複数件がsemanticに正しい場合

ArtifactTypeだけでprovenance source/targetを選択してはならない。

runner draft / Result association / StageExecution association / explicit source identity等、actual provenanceを識別できる情報を使い、

```text
どのPREDICTIONが
どのFITTED_MODELから
実際に導出されたか
```

を一意に対応付けること。

API projectionまたはtestもArtifactTypeだけで任意の1件を選択してはならない。

必要ならexplicit associationに基づくdeterministic projection/testへ修正する。

## 3. 禁止

以下は禁止する。

```text
テストでどのArtifact IDが選ばれてもPASSするように
actual derivationと無関係なCartesian-product lineageを保存すること

created_at/orderの偶然に依存してArtifactを選ぶこと

duplicate Artifactをdict comprehensionで黙って潰して
cardinality defectを隠すこと

G06の最終lineage redesignへscopeを広げること
```

## 4. Correct semantic mapping確定後のみE2Eを実行する

正しいArtifact cardinality / provenance mappingへ修正した後、以下を実行する。

```bash
scripts/test/run_product_postgres_tests.sh \
  tests/product/test_predictive_api_worker_e2e_e3.py::test_predictive_execution_plan_async_worker_results_artifacts_and_lineage
```

correct testによるproduction defectが出た場合は修正し、同じtestをPASSまで再実行する。

PASS後:

```bash
git diff --check
```

をPASSさせる。

その後、C1 production source/testのみをcheckpoint commitする。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C C1 canonical predictive golden path
```

今回停止してよい条件は以下のみ。

```text
PHASE_C_C1_COMPLETE
```

ただしC1 COMPLETEには、

```text
Artifact cardinalityが説明可能
DERIVED_FROMがactual provenanceのみを表す
PostgreSQL E2E PASS
git diff --check PASS
C1 checkpoint commit作成
```

を全て要求する。

最後に:

```text
PHASE_C_C1_COMPLETE
Checkpoint SHA: <full SHA>
PostgreSQL E2E: PASS
Artifact cardinality: <summary>
Provenance mapping: <summary>
```

を報告して停止せよ。

retry / rerun / revise / Phase Dへは進まないこと。
