エージェントに引き渡したコマンドを備忘のために
`````
E4-G05 Trial 01 Phase Cを継続せよ。

Current documentation checkpoint:

```text
2ed9ea4663587b8747a20328dae5e9023777c854
```

Trial番号は変更しない。

Phase Cはまだ `IN_PROGRESS` であり、`DESIGN_BLOCKED` ではない。

現在のin-progress status report:

```text
docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_status_report.md
```

を読み、そこに記載されたactual working stateを引き継ぐこと。

既に実装済みのPhase C変更を巻き戻したり、ゼロから再実装したりしない。

今回は以下を順に完遂する。

```text
1. FITTED_PREPROCESSOR欠落を修正

2. canonical Predictive E2EをPASS

3. retryを完成
   - same Execution identity
   - StageExecution identity preservation
   - attempt-history preservation
   - canonical lifecycle semantics
   - no legacy Family write

4. rerunを完成
   - new canonical Execution
   - base_execution_id relation
   - PREDICTIVE family preservation
   - new canonical StageExecution set
   - no legacy Family write

5. reviseを完成
   - new canonical Execution
   - base_execution_id relation
   - explicit non-empty change_reason
   - changed scientific conditions truthfully represented
   - no legacy Family write

6. Product-facing Predictive operationについてold Family row-count negativeを完成
   - submit
   - read
   - cancel
   - retry
   - rerun
   - revise
   - prefill
   - relevant Result/Artifact operations

7. Phase C専用real PostgreSQL testを完成

8. Phase A / Phase B / relevant G02 / G03 / G04 regressionを実行してPASS

9. git diff --check PASS

10. Phase C production implementation checkpoint commitを作成

11. Phase C checkpoint reportを作成

12. report initial commit SHAを記録し、必要なreport metadata correction commitを作成
```

standard PostgreSQL verificationは必ず以下のrepository標準entry pointのみを使用する。

```bash
scripts/test/run_product_postgres_tests.sh <actual-test-path-or-node> [...]
```

manual Docker / DSN / psql / Alembic workaroundは禁止。

testがFAILした場合、それがcorrect testによるproduction defectなら、原因を修正して再実行すること。

以下を理由に途中停止してはならない。

```text
FITTED_PREPROCESSORの原因を特定しただけ
E2EがまだFAILしている
retry/rerun/reviseの一部だけ完成した
追加testが必要と判明した
regressionが未実行
reportが未作成
Phase D/Eが残っている
G05全体が未完了
```

Phase C完了条件は、06i01に定義されたcompletion criteriaをすべて満たし、

```text
PHASE_C_COMPLETE
```

まで到達することである。

Phase C完了後、このrunではPhase Dへ進まないこと。

最後に必ず:

```text
PHASE_C_COMPLETE
Implementation Checkpoint SHA: <full SHA>
Report Commit SHA: <full SHA>
Report: docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/20_implementation_reports/G05/Trial01/E4-G05_01_PhaseC_implementation_checkpoint_report.md
```

を報告して停止せよ。
`````