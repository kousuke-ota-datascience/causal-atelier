E4-G05 Trial 01 Phase C internal work package C1を継続せよ。

Current source of truth:

```text
docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_06i05.md
```

今回は新しい実装範囲を追加しない。

既に追加済みの以下の修正を検証すること。

```text
PREDICTION Artifact
  GENERATED source Result
  = canonical EVALUATION_RESULT
```

また、既に追加済みの:

```text
PREDICTION --EVIDENCE_FOR--> EVALUATION_RESULT
```

も含めて、canonical output lineage contractを確認する。

以下のstandard PostgreSQL E2Eを再実行せよ。

```bash
scripts/test/run_product_postgres_tests.sh \
  tests/product/test_predictive_api_worker_e2e_e3.py::test_predictive_execution_plan_async_worker_results_artifacts_and_lineage
```

FAILした場合、correct testによるC1 production defectであれば修正し、同じtestをPASSまで再実行すること。

PASS後:

```bash
git diff --check
```

をPASSさせる。

その後、C1で成立したproduction source/testのみをcheckpoint commitする。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C C1 canonical predictive golden path
```

今回進めない。

```text
retry
rerun
revise
full old Family row-count negative
Phase C final report
PHASE_C_COMPLETE
Phase D
Phase E
```

今回停止してよい条件は以下のみ。

```text
PHASE_C_C1_COMPLETE
```

最後に必ず:

```text
PHASE_C_C1_COMPLETE
Checkpoint SHA: <full SHA>
PostgreSQL E2E: PASS
```

を報告して停止せよ。
