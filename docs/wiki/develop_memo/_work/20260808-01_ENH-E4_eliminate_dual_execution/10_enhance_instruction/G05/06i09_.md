E4-G05 Trial 01 Phase Cを継続せよ。

Current C1 checkpoint:

```text
7695834fe2eabc573cd68641c74a76f565334ca1
```

Gate / Trial / Phaseは変更しない。

```text
Gate         = E4-G05
Trial        = 01
Phase        = C
Work package = C2 — Predictive canonical retry
```

C1は `PHASE_C_C1_COMPLETE` として固定済みである。

今回は **Predictive canonical retryのみを完成させること**。

rerun / revise / full authority audit / Phase C final reportには進まない。

## 1. Retry contract

Product-facing Predictive retryはcanonical lifecycle authorityへdelegateすること。

必須contract:

```text
same canonical Execution identity
same canonical StageExecution identity set
retry/attempt history preserved
canonical retry_count semantics preserved
analysis_family remains PREDICTIVE
Predictive specification/plan/seed snapshot preserved

no FamilyExecution write
no FamilyStageExecution write
no FamilyResult destructive reset
no FamilyArtifact destructive reset
```

旧Family lifecycle:

```text
FamilyExecution status reset
FamilyStageExecution recreation/reset
FamilyResult delete
FamilyArtifact delete
```

をcanonical retryとして使用してはならない。

## 2. Actual stateを先に確認

現在の `PredictiveWorkflowService.retry_execution()` とcanonical `ExecutionService` retry contractをactual sourceで確認する。

C1 checkpointにはPhase Cの先行変更が含まれているため、

```text
retryが未実装
```

とは仮定しない。

既に実装されている場合は、そのimplementationを検証・補正する。

## 3. Mandatory real PostgreSQL test

Phase C C2専用testを追加または完成させる。

最低限、real PostgreSQLで:

```text
1. canonical Predictive Executionを作成
2. persistent canonical StageExecutionを作成
3. retry可能な状態へ遷移
4. Product-facing Predictive retryを実行
5. fresh sessionで再読込
```

し、以下をassertする。

```text
[ ] execution_id is unchanged

[ ] StageExecution ID set is unchanged

[ ] retry_count / attempt state changed according to canonical contract

[ ] StageExecution attempt history is preserved / advanced
    according to G03 contract

[ ] analysis_family == PREDICTIVE

[ ] Predictive analysis specification context preserved
[ ] execution plan context preserved
[ ] seed preserved

[ ] no new FamilyExecutionOrm row
[ ] no new FamilyStageExecutionOrm row

[ ] canonical Result / Artifact ownership contract is not destroyed
    by legacy delete/reset behavior
```

必要なnegative caseも追加する。

最低限:

```text
cross-project execution -> rejected
non-PREDICTIVE canonical execution -> Predictive retryから取得不可
retry-ineligible state -> canonical lifecycle ruleでrejected
```

## 4. Standard PostgreSQL runner

唯一のentry point:

```bash
scripts/test/run_product_postgres_tests.sh <actual-C2-test-path-or-node> [...]
```

を使用する。

manual Docker / DSN / psql / Alembic workaroundは禁止。

correct testによるproduction defectが出た場合、修正して同じtestをPASSまで再実行すること。

## 5. C1 regression

C2修正後、少なくともC1 Golden Path E2Eも再実行する。

```bash
scripts/test/run_product_postgres_tests.sh \
  tests/product/test_predictive_api_worker_e2e_e3.py::test_predictive_execution_plan_async_worker_results_artifacts_and_lineage
```

C1で成立した以下を壊してはならない。

```text
canonical Predictive Golden Path
Artifact cardinality = 4
typed Result / Artifact semantics
actual provenance chain
canonical ownership
```

## 6. Completion criteria

以下を全て満たす。

```text
[ ] Product-facing Predictive retry uses canonical lifecycle authority
[ ] same Execution identity PASS
[ ] stable StageExecution identity PASS
[ ] attempt/retry semantics PASS
[ ] Predictive snapshot semantics preserved
[ ] no Family lifecycle write PASS
[ ] retry negative cases PASS
[ ] C1 Golden Path regression PASS
[ ] standard PostgreSQL runner PASS
[ ] git diff --check PASS
```

## 7. C2 checkpoint commit

全criteria PASS後、C2で成立したproduction source/testをcheckpoint commitする。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C C2 canonical predictive retry
```

commit前:

```bash
git status --short
git diff --check
git diff --cached --name-status
```

commit後:

```bash
git rev-parse HEAD
git status --short
```

を確認する。

## 8. 今回進めない

```text
rerun
revise completion
full old Family row-count matrix
Phase C final regression
Phase C checkpoint report
PHASE_C_COMPLETE
Phase D
Phase E
```

## 9. Stop condition

今回停止してよい条件は以下のみ。

```text
PHASE_C_C2_COMPLETE
```

最後に必ず:

```text
PHASE_C_C2_COMPLETE
Checkpoint SHA: <full SHA>
Retry PostgreSQL test: PASS
C1 Golden Path regression: PASS
Execution identity: preserved
StageExecution identity: preserved
```

を報告して停止せよ。
