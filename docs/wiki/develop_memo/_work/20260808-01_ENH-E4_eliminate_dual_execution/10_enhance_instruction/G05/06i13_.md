# E4-G05 Trial 01 Phase C C4 — Final Authority Audit / Regression / Completion

E4-G05 Trial 01 Phase Cを継続せよ。

Current C3b implementation checkpoint:

```text
7870329192986bcd070935bf48fe814dda444a77
```

Current source of truth:

```text
docs/wiki/develop_memo/_work/
20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_06i12.md
```

Gate / Trial / Phaseは変更しない。

```text
Gate         = E4-G05
Trial        = 01
Phase        = C
Work package = C4 — Final Predictive authority audit / regression / Phase C completion
```

以下は完了済みとして固定する。

```text
C1  Predictive canonical Golden Path   COMPLETE
C2  Predictive canonical retry         COMPLETE
C3a Predictive canonical rerun         COMPLETE
C3b Predictive canonical revise        COMPLETE
```

今回は **Phase C全体を最終監査し、`PHASE_C_COMPLETE` まで閉じること**。

Phase Dには進まない。

---

## 1. Start from actual repository state

最初にactual repository stateを確認する。

```bash
git branch --show-current
git rev-parse HEAD
git status --short
git diff --check
git merge-base --is-ancestor \
  7870329192986bcd070935bf48fe814dda444a77 HEAD
echo $?
git log -12 --oneline
```

C3b後に未コミット変更が存在する場合はactual内容を確認する。

既存C1/C2/C3a/C3b実装を巻き戻さない。

---

## 2. C4の目的

Phase C completionのprimary questionは以下。

> new user-visible Predictive Product operationについて、Execution identity / Stage lifecycle / Result / Artifact / lifecycle mutationのauthorityがcanonical Product aggregate以外へ逃げる経路が残っていないか。

Phase Cで確認する範囲:

```text
submit
get/list Execution
get StageExecution
get Result
get Artifact
lineage projection
prefill
cancel
retry
rerun
revise
canonical worker Golden Path
```

これらについてnew canonical Predictive Executionが:

```text
FamilyExecutionOrm
FamilyStageExecutionOrm
FamilyResultOrm
FamilyArtifactOrm
```

をauthorityとして必要としないことを証明する。

---

## 3. Actual authority inventory

actual sourceを再走査し、以下のmatrixを作る。

```text
Surface                     Expected authority

submit                      canonical Execution
execution read              canonical Execution
stage read                  canonical StageExecution
result read                 canonical Result
artifact read               canonical Artifact
lineage owned IDs           canonical Execution/Result/Artifact IDs
prefill                     canonical Execution snapshot
cancel                      canonical Execution lifecycle
retry                       canonical Execution + StageExecution lifecycle
rerun                       canonical base Execution
revise                      canonical base Execution
worker claim                canonical claim authority
worker output               canonical Result/Artifact
```

各surfaceについてactual implementation locationを確認する。

checkpoint reportに:

```text
surface
entry point
canonical authority
remaining legacy code if any
whether reachable from new Product flow
```

を記録する。

---

## 4. No silent dual-read authority

new canonical Predictive IDについて:

```text
canonical lookup miss
    -> FamilyExecutionOrm
    -> FamilyResultOrm
    -> FamilyArtifactOrm
```

のようなsilent fallbackを許可しない。

最低限以下を監査・testする。

```text
unknown canonical execution_id
unknown canonical result_id where applicable
unknown canonical artifact ownership lookup where applicable
```

Expected:

```text
canonical not found / rejected
```

Forbidden:

```text
old Family tableへfallbackして成功
```

historical pre-G05 compatibility pathが残る場合でも:

```text
explicit
bounded
new canonical IDとauthorityが衝突しない
```

こと。

---

## 5. Product-facing old Family write negative matrix

C4ではPhase C全体のoperation matrixとしてold Family table row-count negativeを完成させる。

対象:

```text
submit
execution read
stage read
result read
artifact read
lineage read
prefill
cancel
retry
rerun
revise
canonical worker execution
```

対象table:

```text
FamilyExecutionOrm
FamilyStageExecutionOrm
FamilyResultOrm
FamilyArtifactOrm
```

各operationについて必要なbefore / after evidenceを取る。

Primary invariant:

```text
new Product-facing Predictive operationによる
new Family authority row write = 0
```

read operationで当然row countが変わらないことだけを形式的に並べるのではなく、write可能性があるsurface:

```text
submit
cancel
retry
rerun
revise
worker execution
```

を重点的に証明する。

---

## 6. Canonical failure must not fall back to legacy authority

Phase C scope内で少なくとも以下を確認する。

canonical submission / lifecycle / worker processingでproduction errorが起きたとき:

```text
canonical path failed
    -> old FamilyExecution pathへfallback
```

してはならない。

既存source inspection + targeted automated negativeで証明できる範囲を証明する。

ただしlegacy claim/process method自体の全面shutdownはPhase D。

C4で要求するのは:

```text
new canonical Predictive Product flowがfailure時にlegacy authorityへ逃げない
```

こと。

---

## 7. Canonical worker compatibility audit

C1で成立したGolden PathをPhase C final stateでも維持する。

確認:

```text
canonical claim
persistent canonical StageExecution
Predictive scientific runner
typed canonical Results
typed canonical Artifacts
canonical ownership
canonical lineage projection
terminal SUCCEEDED
```

Artifact invariant:

```text
PARTITION_INDEX       exactly 1
FITTED_PREPROCESSOR   exactly 1
FITTED_MODEL          exactly 1
PREDICTION            exactly 1
```

provenance:

```text
FITTED_PREPROCESSOR --DERIVED_FROM--> PARTITION_INDEX
FITTED_MODEL        --DERIVED_FROM--> FITTED_PREPROCESSOR
PREDICTION          --DERIVED_FROM--> FITTED_MODEL
PREDICTION          --EVIDENCE_FOR--> EVALUATION_RESULT
```

を維持する。

---

## 8. Mutation semantics final audit

### cancel

```text
canonical Execution mutation
canonical StageExecution cancellation semantics
no Family mutation
```

### retry

```text
same Execution identity
stable StageExecution identities
attempt history preserved
no legacy destructive Result/Artifact reset
```

### rerun

```text
new Execution
base_execution_id
revision_kind = RERUN
change_reason = NONE
new StageExecution identities
base non-destructive
```

### revise

changed condition:

```text
new Execution
base_execution_id
revision_kind = REVISED
explicit non-empty change_reason
truthful changed scientific snapshot
```

same condition:

```text
canonical comparison result = RERUN
change_reason = NONE
```

service/routerがcanonical comparison結果を偽装していないこと。

---

## 9. Predictive read projection final audit

以下がcanonical sourceからprojectionされること。

```text
Execution
StageExecution
Result
Artifact
prefill
lineage
```

少なくともPhase Aで追加したtyped Result / Artifact semanticsについてgeneric degradationがないこと。

Result examples:

```text
SPLIT_RESULT
TRAINING_RESULT
EVALUATION_RESULT
ERROR_ANALYSIS_RESULT
PREDICTIVE_EXPLANATION_RESULT
MODEL_CARD_RESULT
```

Artifact examples:

```text
PARTITION_INDEX
FITTED_PREPROCESSOR
FITTED_MODEL
PREDICTION
PREDICTIVE_EXPLANATION
MODEL_CARD
```

actual Golden Pathで存在するものをassertする。

---

## 10. Phase C final automated test

Phase C final authority audit用testを追加する。

推奨:

```text
tests/product/
test_enh_e4_g05_phase_c_authority_audit_postgres.py
```

このtestでは既存C1/C2/C3a/C3b testを重複実装せず、Phase C全体で残りやすいcross-surface invariantを検証する。

最低限:

```text
canonical Product-facing Predictive authority
no silent fallback
no legacy new write
cross-surface project/family isolation
canonical read after lifecycle mutations
```

を検証する。

---

## 11. Phase C regression bundle

C4ではPhase C internal work packagesをまとめてstandard PostgreSQL runnerで実行する。

最低限:

```text
C1 Golden Path
C2 retry
C3a rerun
C3b revise
C4 authority audit
```

actual test paths:

```text
tests/product/test_predictive_api_worker_e2e_e3.py::
test_predictive_execution_plan_async_worker_results_artifacts_and_lineage

tests/product/test_enh_e4_g05_phase_c_retry_postgres.py

tests/product/test_enh_e4_g05_phase_c_rerun_postgres.py

tests/product/test_enh_e4_g05_phase_c_revise_postgres.py

<actual C4 authority audit test>
```

---

## 12. Phase A / Phase B regression

Phase C completionにはPhase A/Bを壊していない証拠を含める。

### Phase A

```text
typed Result / Artifact canonical persistence
schema/version preservation
Result ↔ Artifact association
constraint / PostgreSQL round-trip
```

### Phase B

```text
Exploratory canonical Result read
Exploratory downstream draft
no old shadow write
```

actual existing test pathを確認してstandard runnerへ含める。

---

## 13. G02 / G03 / G04 relevant regression

Phase Cはpassed Gate contractに依存するため、関連regressionを実行する。

### G02

```text
canonical Execution identity
claim/lifecycle
retry/rerun/revise semantics
```

### G03

```text
persistent StageExecution
stable identity
attempt history
```

### G04

```text
Result ownership
Artifact ownership
Result ↔ Artifact association
typed reuse / physical-store boundary
```

既存passed Gate testをactual repositoryから選定する。

不要に全test suiteを無差別実行する必要はない。

ただしPhase Cが触ったcontractを十分coverすること。

---

## 14. Standard PostgreSQL verification

real PostgreSQL evidenceは唯一:

```bash
scripts/test/run_product_postgres_tests.sh <pytest paths/nodes> [...]
```

を使う。

C4 final verificationでは、関連testを可能な範囲で**1つのcopy-pastable command**へまとめる。

reportには省略せず完全なcommandを書く。

禁止:

```text
same as previous
上記と同じ
C1 commandを参照
```

各evidenceに必要なcommandは完全形で記載する。

manual:

```text
docker
DSN
psql
Alembic
external ad-hoc PostgreSQL pytest
```

は禁止。

---

## 15. Non-PostgreSQL regression

関連するunit / boundary / API testも実行する。

最低限actual Phase C変更に関係する:

```text
Predictive router
dependency construction
ExecutionService revision semantics
canonical submission convergence
worker dispatch
```

をcoverする。

exact command / exit code / pass countを記録する。

---

## 16. Migration audit

Expected Product migration head:

```text
20260809_product_0010
```

Phase Cで追加migrationが存在しないことを確認する。

追加されている場合は:

```text
revision
down_revision
reason
domain/ORM/schema consistency
```

をreportする。

Phase C final auditのためだけに不要なmigrationを追加しない。

---

## 17. Phase C completion criteria

以下を全てDONEにする。

```text
[ ] Predictive authority inventory complete

[ ] submit canonical authority PASS
[ ] Execution read canonical authority PASS
[ ] StageExecution read canonical authority PASS
[ ] Result read canonical authority PASS
[ ] Artifact read canonical authority PASS
[ ] lineage owned IDs canonical PASS
[ ] prefill canonical snapshot PASS

[ ] cancel canonical mutation PASS
[ ] retry canonical mutation PASS
[ ] rerun canonical mutation PASS
[ ] revise canonical mutation PASS

[ ] unknown canonical ID has no silent Family fallback

[ ] canonical Product failure does not fallback to legacy authority

[ ] Product-facing Predictive old Family write negative matrix PASS

[ ] worker Golden Path PASS
[ ] typed Result semantics PASS
[ ] typed Artifact semantics PASS
[ ] Artifact cardinality / provenance PASS

[ ] C1 regression PASS
[ ] C2 regression PASS
[ ] C3a regression PASS
[ ] C3b regression PASS
[ ] C4 authority audit PASS

[ ] Phase A regression PASS
[ ] Phase B regression PASS

[ ] relevant G02 regression PASS
[ ] relevant G03 regression PASS
[ ] relevant G04 regression PASS

[ ] relevant unit/boundary/API regression PASS

[ ] Product PostgreSQL migration head verified

[ ] git diff --check PASS

[ ] Phase C final implementation checkpoint commit created
[ ] Phase C implementation checkpoint report created
[ ] report initial commit SHA recorded
```

---

## 18. Phase C final implementation checkpoint commit

全production/test criteriaがPASSした後、Phase C final implementation checkpoint commitを作成する。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C complete
```

このcommitには:

```text
C4 production correction if any
C4 automated tests
Phase C final test adjustments
```

を含める。

Phase C checkpoint reportはimplementation checkpoint commitの後に作る。

commit前:

```bash
git status --short
git diff --check
git diff --cached --name-status
```

commit後:

```bash
git rev-parse HEAD
git status --short
```

を記録する。

このSHAを:

```text
Phase checkpoint implementation commit
```

としてreportへ固定する。

---

## 19. Phase C Implementation Checkpoint Report

instruction/in-progress reportへ実行結果を追記しない。

以下を作成する。

```text
docs/wiki/develop_memo/_work/
20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC_implementation_checkpoint_report.md
```

これはPhase C final checkpoint reportである。

まだG05 Gate-level:

```text
E4-G05_01_implementation_completion_report.md
```

ではない。

---

## 20. Phase C checkpoint report required metadata

最低限:

```text
# E4-G05 Trial 01 Phase C Implementation Checkpoint Report

- Project
- Enhancement
- Gate
- Trial
- Phase
- Phase Status: PHASE_C_COMPLETE | DESIGN_BLOCKED
- Branch
- Phase baseline checkpoint
- C1 checkpoint
- C2 checkpoint
- C3a checkpoint
- C3b checkpoint
- Phase C final implementation checkpoint
- Report commit
- Migration head
- Started at
- Finished at
```

値がないfieldを削除しない。

```text
N/A
NONE
NOT_RUN
UNKNOWN
```

を使用する。

---

## 21. Phase C checkpoint report required sections

```text
## 1. Input

## 2. Phase C Scope Summary

## 3. Internal Checkpoint Ledger
### C1
### C2
### C3a
### C3b
### C4

## 4. Final Authority Matrix

## 5. Files Changed
### Added
### Modified
### Deleted

## 6. Implementation Details

## 7. Automated Test Code Added / Changed

## 8. Migration

## 9. No-Legacy-Write / No-Fallback Evidence

## 10. Passed-Gate Regression Impact
### G02
### G03
### G04
### Phase A
### Phase B

## 11. Known Limitations / Remaining G05 Work

## 12. Explicit Out-of-Scope Work

## 13. Git Evidence

## 14. Phase Verification Evidence
- exact commands
- exit codes
- pass/fail/skip counts
- evidence paths
- expected
- actual
- Facts
- Interpretation

## 15. Next-Phase Handoff
- Next phase: Phase D
- Ready for Phase D: YES / NO
- Gate READY_FOR_TEST: NO

## 16. Design Block
```

Substantive test success does not waive report-format compliance.

---

## 22. Report commit procedure

1. Phase C final implementation checkpoint commitを作成。
2. reportを作成し:

```text
Report commit: PENDING
```

とする。
3. report initial commitを作成。

Suggested:

```text
E4-G05 Trial 01 Phase C checkpoint report
```

4. SHA取得:

```bash
git rev-parse HEAD
```

5. `Report commit: PENDING` をinitial report commit SHAへ置換。
6. metadata correction commitを作成。

Suggested:

```text
E4-G05 Trial 01 Phase C report metadata
```

Report commit fieldは:

```text
reportを最初にrepositoryへ導入したcommit
```

を意味する。

self-referential SHAを要求しない。

---

## 23. Phase Cで閉じるもの / 閉じないもの

Phase Cで閉じる:

```text
Predictive Product-facing canonical convergence
Predictive canonical read projection
Predictive canonical mutation delegation
Predictive canonical worker compatibility
Predictive old Family new-write negative
```

Phase Cで閉じない:

```text
global legacy claim/process hard shutdown
all familiesのlegacy method reachability shutdown
global canonical failure fallback audit
legacy source deletion
```

これらはPhase D。

また以下もまだ行わない。

```text
TD-001 / TD-002 / TD-003 final closure
G05 final Golden Paths
G05 final implementation completion report
READY_FOR_TEST
```

これらはPhase E。

---

## 24. Stop condition

今回停止してよいのは:

```text
PHASE_C_COMPLETE
```

のみ。

以下を全て満たすこと。

```text
C4 completion criteria all DONE
Phase C final regression PASS
Phase C final implementation checkpoint created
Phase C checkpoint report created
report initial commit SHA recorded
```

最後に必ず:

```text
PHASE_C_COMPLETE
Implementation Checkpoint SHA: <full SHA>
Report Commit SHA: <full SHA>
Report: docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/20_implementation_reports/G05/Trial01/E4-G05_01_PhaseC_implementation_checkpoint_report.md
Next Phase: D
Gate READY_FOR_TEST: NO
```

を報告して停止せよ。

---

## 25. Stop reasons NOT accepted

以下を途中停止理由として認めない。

```text
authority auditでlegacy referenceを発見した
negative test追加が必要
regressionでFAILした
report作成が残っている
report metadata commitが残っている
Phase Dがまだ残っている
Phase Eがまだ残っている
G05全体が未完了
```

correct testによるPhase C production defectはC4内で修正して再検証する。

---

E4-G05 Trial 01 Phase C C4を完遂し:

```text
PHASE_C_COMPLETE
```

まで到達せよ。

Phase C完了後、このrunではPhase Dへ進まないこと。
