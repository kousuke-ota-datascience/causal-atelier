E4-G05 Trial 01 Phase Cを継続せよ。

Current documentation checkpoint:

```text
bf90769fa2ebc9b2dd7b7aa9b021409801feb46f
```

Trial番号、Gate、Phaseは変更しない。

```text
Gate  = E4-G05
Trial = 01
Phase = C
```

今回はPhase C全体を完了しようとしない。

**Phase C internal work package C1 — Predictive canonical Golden Pathのみを完了すること。**

現在の状態は以下のreportをsource of truthとして引き継ぐ。

```text
docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_06i02.md
```

既存の未コミットPhase C production変更を巻き戻さないこと。

## 今回の作業範囲

現在の直接的な未検証修正:

```text
canonical Predictive Artifact output filename
.bin -> .json
```

によってArtifact media typeが:

```text
application/json
```

として永続化・projectionされることを検証する。

そのうえで、以下のcanonical Predictive E2E testをstandard PostgreSQL runnerでPASSさせる。

```bash
scripts/test/run_product_postgres_tests.sh \
  tests/product/test_predictive_api_worker_e2e_e3.py::test_predictive_execution_plan_async_worker_results_artifacts_and_lineage
```

必要なproduction defectを発見した場合は、Phase C C1 scope内で修正し、同じtestを再実行してPASSさせること。

PASS時に最低限以下を確認する。

```text
[ ] canonical Predictive Execution created
[ ] canonical StageExecution created
[ ] canonical worker execution reaches SUCCEEDED

[ ] typed Predictive Results persisted

[ ] Artifact types include:
    PARTITION_INDEX
    FITTED_PREPROCESSOR
    FITTED_MODEL
    PREDICTION

[ ] Artifact media type is correct
[ ] Artifact ownership is canonical
[ ] StageExecution association is canonical
[ ] schema/version metadata is preserved

[ ] no FamilyExecution shadow row required for this Golden Path
[ ] no FamilyStageExecution shadow row required for this Golden Path

[ ] standard PostgreSQL runner exit code = 0
[ ] git diff --check PASS
```

## 今回やらないこと

以下には進まない。

```text
retry
rerun
revise
full old Family row-count matrix
Phase A/B/G02/G03/G04 full regression
Phase C final checkpoint report
PHASE_C_COMPLETE
Phase D
Phase E
```

これらは後続のPhase C internal work packageで扱う。

## Checkpoint commit

上記C1 completion criteriaを全て満たしたら、現在working tree上にあるPhase C production変更のうち、C1までに成立したproduction source/testをcheckpoint commitする。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C C1 canonical predictive golden path
```

commit前:

```bash
git status --short
git diff --check
git diff --cached --name-status
```

commit後:

```bash
git rev-parse HEAD
git status --short
```

を確認する。

user/documentation作業をproduction checkpointへ不用意に混入させないこと。

## Allowed stop condition

今回停止してよいのは:

```text
PHASE_C_C1_COMPLETE
```

のみ。

最後に必ず:

```text
PHASE_C_C1_COMPLETE
Checkpoint SHA: <full SHA>
PostgreSQL test: PASS
```

を報告して停止せよ。

これは:

```text
PHASE_C_COMPLETE
READY_FOR_TEST
```

ではない。

C1完了後、このrunではretryへ進まないこと。
