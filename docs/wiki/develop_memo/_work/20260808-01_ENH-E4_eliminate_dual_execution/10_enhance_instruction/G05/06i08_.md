E4-G05 Trial 01 Phase C C1を継続せよ。

Current source of truth:

```text
docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_06i07.md
```

06i07で追加済みのArtifact cardinality / actual provenance修正を検証する。

以下をstandard PostgreSQL runnerで実行せよ。

```bash
scripts/test/run_product_postgres_tests.sh \
  tests/product/test_predictive_api_worker_e2e_e3.py::test_predictive_execution_plan_async_worker_results_artifacts_and_lineage
```

PASS時に最低限以下が成立していること。

```text
Artifact total = 4

PARTITION_INDEX       exactly 1
FITTED_PREPROCESSOR   exactly 1
FITTED_MODEL          exactly 1
PREDICTION            exactly 1

DERIVED_FROM:
FITTED_PREPROCESSOR -> PARTITION_INDEX
FITTED_MODEL        -> FITTED_PREPROCESSOR
PREDICTION          -> FITTED_MODEL

PREDICTION --EVIDENCE_FOR--> EVALUATION_RESULT

canonical Execution / StageExecution / Result / Artifact ownership maintained
```

FAILした場合、correct testによるC1 production defectであれば修正し、同一E2EをPASSまで再実行すること。

PASS後:

```bash
git diff --check
```

を実行しPASSさせる。

その後、C1 production source/testのみをpath-specific stagingしてcheckpoint commitする。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C C1 canonical predictive golden path
```

今回停止してよい条件は以下のみ。

```text
PHASE_C_C1_COMPLETE
```

最後に:

```text
PHASE_C_C1_COMPLETE
Checkpoint SHA: <full SHA>
PostgreSQL E2E: PASS
Artifact cardinality: 4 / each type exactly 1
Provenance mapping: actual one-to-one chain PASS
```

を報告して停止せよ。

retry / rerun / revise / C2以降へは進まないこと。
