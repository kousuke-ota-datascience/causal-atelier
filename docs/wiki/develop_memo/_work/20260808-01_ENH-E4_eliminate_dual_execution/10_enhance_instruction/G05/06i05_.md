E4-G05 Trial 01 Phase C internal work package C1を継続せよ。

Current source of truth:

```text
docs/wiki/develop_memo/_work/20260808-01_ENH-E4_eliminate_dual_execution/
20_implementation_reports/G05/Trial01/
E4-G05_01_PhaseC__in_progress_06i04.md
```

Gate / Trial / Phaseは変更しない。

```text
Gate  = E4-G05
Trial = 01
Phase = C
Work package = C1
```

今回は以下だけを完遂する。

1. canonical output lineageについて、

```text
PREDICTION --EVIDENCE_FOR--> EVALUATION_RESULT
```

が既存Predictive E2E contractどおり保存されるよう補完する。

2. 既に追加済みの以下のcanonical output lineage修正も含めて検証する。

```text
Execution --GENERATED--> Result

Result/Execution --GENERATED--> Artifact

PARTITION_INDEX
  -> FITTED_PREPROCESSOR
  -> FITTED_MODEL
  -> PREDICTION

PREDICTION --EVIDENCE_FOR--> EVALUATION_RESULT
```

3. 以下のstandard PostgreSQL E2Eを再実行し、PASSまで修正・再実行する。

```bash
scripts/test/run_product_postgres_tests.sh \
  tests/product/test_predictive_api_worker_e2e_e3.py::test_predictive_execution_plan_async_worker_results_artifacts_and_lineage
```

4. PASS後、

```bash
git diff --check
```

をPASSさせる。

5. C1で成立したproduction source/testのみをcheckpoint commitする。

Suggested commit message:

```text
E4-G05 Trial 01 Phase C C1 canonical predictive golden path
```

commit前:

```bash
git status --short
git diff --check
git diff --cached --name-status
```

commit後:

```bash
git rev-parse HEAD
git status --short
```

を確認する。

今回進めない。

```text
retry
rerun
revise
full old Family row-count negative
Phase C final checkpoint report
PHASE_C_COMPLETE
Phase D
Phase E
```

今回停止してよい条件は以下のみ。

```text
PHASE_C_C1_COMPLETE
```

最後に必ず:

```text
PHASE_C_C1_COMPLETE
Checkpoint SHA: <full SHA>
PostgreSQL E2E: PASS
```

を報告して停止せよ。
