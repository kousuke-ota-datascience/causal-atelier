# Historical Schema Note — v1.1 compatibility

Retained as provenance category for the upstream workflow template.

ENH-E7 does not rely on v1.1 behavior where it conflicts with the current v2 workflow semantics.
