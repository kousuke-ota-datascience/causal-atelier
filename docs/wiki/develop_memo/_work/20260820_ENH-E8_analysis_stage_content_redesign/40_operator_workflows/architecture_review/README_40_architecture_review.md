# Architecture Review

Architecture Reviewはconditional workflowである。

runtime lifecycle、authority/ownership、persistence/schema、source-of-truth、legacy-path integration等のarchitecture boundaryを変更する必要が判明した場合に使用する。

現在のENH-E8では`N/A`。
