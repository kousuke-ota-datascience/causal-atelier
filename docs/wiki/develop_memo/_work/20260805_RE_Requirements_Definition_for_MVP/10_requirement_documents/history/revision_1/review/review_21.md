## 3. Entity定義

各エンティティ間のFK制約やリレーションが見たい。1対何で対応しているのか？などが見たい
ER図があればよい。

主要属性に以下の項目を付与し、表形式で表現すること
- PK: 1 または無記入。
- FK: 参照元テーブル名称を記載
- データ型
- NOT NULL 制約
- その他制約
- 説明

## 4. 導出モデル

- 名前は、 `## 3. Entity定義`と対応させるように  `## 4. 非Entityのデータモデル`とでもする。
    - `### 4.0. overview` を作り、「正本Entityではないが、主要なが因縁データモデルについて記載する」旨を記載する
- どのようにして導出するのかのロジックも記載しておく
- `## 4.1 Comparison Projection` 見出しのシャープが少ない

## 5. 状態モデル
これはあくまでもexecutionの中のstatusがどのような値をとるのか？それはどのような遷移をたどるのか・ということ？