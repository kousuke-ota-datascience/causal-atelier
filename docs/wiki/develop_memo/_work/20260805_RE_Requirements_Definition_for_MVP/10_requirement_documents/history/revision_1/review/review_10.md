## 6. カバーするアルゴリズム


- 現行コードに関する記載はいらない。なぜならばコードベース修正後の世界において、「この要件定義書を見ればわかる状態」を目指すからである。そこには、現行コード（=コードベース修正後は「前身コード」）
に関する知識がなくても理解できるようにドキュメントを整備しておく必要がある

- しいていうなれば、 ## 6.2. において


| 分類 | Algorithm / Estimator | 初期UIでの扱い | 前身コードからの流用有無 |
|---|---|---|---|
| Discovery | PC | MUST | 流用 |

コーディングエージェントに対する前身コードからの流用有無についての指示は、別途コーディングプロンプトという形で記載すること


## 7. インターフェース要件

### 7.3 CLI

> - CLI実行はAriadne `execution_id`を生成しないこと

ここだけ記載の粒度が細かすぎないか？インターフェース要件の粒度で書くならば、

> CLIとweb/API の実行粒度は独立であり識別用のIDを共有しないこと。物理レイヤに近い表現でいうなれば、webアプリ実行時に発行される `execution_id` は、CLI実行時は生成しないこと。

という記載になるのではないか？



## 8. 非機能要件