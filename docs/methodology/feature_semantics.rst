Feature Semantics
=================

Feature semantics describe each feature's causal role, source table, source
column, aggregation, transform, unit ID, and adjustment eligibility.

The runtime compares discovery-derived feature semantics with inference
semantics and treats mismatches as errors.
