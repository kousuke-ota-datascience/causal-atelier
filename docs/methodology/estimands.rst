Estimands
=========

ATE and ATT are explicit causal targets. Estimators are named by target when
they estimate a causal estimand under adjustment assumptions, for example
``g_computation_ate`` and ``ipw_att``.

``ols_coefficient`` is a regression coefficient. It is not reported as ATE or
ATT.
